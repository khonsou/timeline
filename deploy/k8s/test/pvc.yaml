apiVersion: v1
kind: PersistentVolume
metadata:
  name: timeline-board-nas
  labels:
    alicloud-pvname: timeline-board-nas
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: nasplugin.csi.alibabacloud.com
    volumeHandle: timeline-board-nas
    volumeAttributes:
      server: 2666949dcf-yyf68.cn-shenzhen.nas.aliyuncs.com
      path: /timeline-board
  mountOptions:
    - nolock,tcp,noresvport
    - vers=3
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timeline-board-data
  namespace: angrymiao-test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  selector:
    matchLabels:
      alicloud-pvname: timeline-board-nas
